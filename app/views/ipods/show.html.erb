<div class="row container show-page">
  <div class="image contain col-8">
  <% if @ipod.photo.attached? %>
    <%= cl_image_tag @ipod.photo.key, class:'card-img' %>
  <% else %>
    <%= image_tag "#{@ipod.model}.jpg", class:'image'  %>
  <% end %>
  </div>

  <div class="col-4 content">
    <div class="d-flex">
    <p class="h6 lead font-weight-light"><i class="fa fa-thin fa-money-bills me-2"></i><%= @ipod.price %>€</p>
    </div>
    <h1 class="text-uppercase"><%= @ipod.name %></h1>
    <p class="text-muted"><%= @ipod.description %></p>
    <p class="h6 font-weight-light text-muted text-uppercase">Details</p>
    <br>
    <div class="d-flex">
    <p class="title font-weight-bold me-2">Model:</p>
    <p><%= @ipod.model %></p>
    </div>
    <div class="d-flex">
    <p class="title font-weight-bold me-2">State:</p>
    <p><%= @ipod.state %></p>
    </div>
    <div class="d-flex">
    <p class="title font-weight-bold me-2">Color:</p>
    <p><%= @ipod.color %></p>
    </div>
    <div class="d-flex">
    <p class="title font-weight-bold me-2">Capacity:</p>
    <p><%= @ipod.capacity %></p>
    </div>
<br>
    <a class="btn btn-dark shadow-sm w-100 mb-2" href="#" role="button"><i class="fa-solid fa-bag-shopping"></i>  l  Buy</a>
    <a class="btn btn-outline-dark shadow-sm w-100" href="#" role="button">Back to List</a>
</div>
</div>
